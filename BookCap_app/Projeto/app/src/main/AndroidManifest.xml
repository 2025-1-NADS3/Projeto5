<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bookcap"
        tools:targetApi="31">

        <activity
            android:name=".ActivityPagamento"
            android:exported="false" />
        <!-- Tela que lista feedbacks -->
        <activity
            android:name=".ActivityVerFeedbacks"
            android:exported="false" />

        <!-- Tela de cadastro de usuário -->
        <activity
            android:name=".ActivityCadastro"
            android:exported="false" />

        <!-- Tela de login será a inicial -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Demais telas -->
        <activity
            android:name=".ActivityFeedbacks"
            android:exported="false" />
        <activity
            android:name=".ActivityExtrato"
            android:exported="false" />
        <activity
            android:name=".ActivityHistorico"
            android:exported="false" />
        <activity
            android:name=".ActivityLivroDetalhe"
            android:exported="false" />
        <activity
            android:name=".ActivityBiblioteca"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false" />

    </application>
</manifest>
